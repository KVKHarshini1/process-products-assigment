<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DOM Manipulation Example</title>
</head>
<body>

    <h1 id="title">Hello User!</h1>

    <p>This is paragraph 1.</p>
    <p>This is paragraph 2.</p>
    <p>This is paragraph 3.</p>

    <div class="container">
        <p>Inside container</p>
    </div>

    <script>
        // 1️⃣ Select <h1> by id and change text
        let heading = document.getElementById("title");
        heading.textContent = "Welcome to the DOM World!";

        // 2️⃣ Select all <p> elements and set text color to blue
        let paragraphs = document.getElementsByTagName("p");
        for (let i = 0; i < paragraphs.length; i++) {
            paragraphs[i].style.color = "blue";
        }

        // 3️⃣ Select first <div class="container"> using querySelector and change bg color
        let box = document.querySelector(".container");
        box.style.backgroundColor = "yellow";
    </script>

</body>
</html>
